const diff = require('./index')

diff('./test-docs/template-a.pdf', './test-docs/template-c.pdf')
  .then(diff => {
    console.log('diff :', diff)
  })
  .catch(err => {
    console.log('Error:', err)
  })
